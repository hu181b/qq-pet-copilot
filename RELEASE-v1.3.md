# QQ 宠物助手 v1.3

Windows x64 / Android 真机便携版。基于 [原项目](https://github.com/490720818/qq-pet-copilot) 维护，保留原作者贡献、上游历史与 GPL-3.0 许可证。

## 下载与使用

下载本次 Release 的 **QQPetCopilot-v1.3-windows-x64.zip**，完整解压到有写入权限的目录，双击 **QQPetCopilot.exe**。不要下载 GitHub 自动生成的 Source code 压缩包来当成可运行程序，也不要直接在压缩包预览里启动。

Python、Qt、ADB、scrcpy 和三个 OCR 模型已内置，无需另外安装这些运行组件。手机需要连接电脑、开启并授权 USB 调试，部分机型还需允许 USB 调试输入控制；电脑需要能识别手机。登录 QQ、打开宠物页面后，在助手中检查设备和任务设置，再点击“开始”。不包含 QQ 客户端、账号或手机驱动安装程序。

首次启动自动生成默认配置和运行数据，不附带维护者的个人设置、账号、设备序列号、通知密钥、历史统计或截图。任务不会因打开窗口而自动开始。更新已有安装时，先退出旧程序，保留 `config.yaml`、`profiles.json`、`profiles/`、`runs/`，再替换 EXE。

## 相比 v1.0 的更新

- 修复运行中切换配置时提示框无法关闭的问题，使用独立对话框；仍需先停止调度再切换配置。
- 设置、进度、状态和配置索引统一使用独立临时文件与原子替换，避免序列化或写入失败留下半份文件。
- 修复移除任务后仍被调度、重复任务顺序、等待期间热加载延迟的问题；保留进行中任务的后续结算。
- 结算时缺少退出按钮会保留待结算状态并重试，避免过早计数或丢失待结算任务。
- 改善损坏进度、异常状态缓存、配置索引和护理阈值的校验与处理。
- 新增宠物爪印程序图标和任务栏图标，保留窗口缩放和关闭按钮行为。

同时保留 v1.0 的真机支持、护理/打工/学园导航修复、独立多配置、单线程 OCR、后台暂停视频及较低调度优先级等优化。后台托管继续；其他软件满载时仍可能有资源竞争。

## 数据与验证边界

原子替换不提供跨进程读改写事务锁，不代表断电后一定保留最新数据。不要让多个实例同时编辑同一份配置。源码中的回归测试隔离手机操作；缺失的私人截图明确跳过，不作为通过项。未穷尽全部真机长时任务及所有驱动环境。

发行包只包含 EXE、LICENSE 和本说明；可用同页 SHA256SUMS.txt 校验 ZIP。源码仓库包含通用默认模板 `config.example.yaml`，不包含任何用户生成的 `config.yaml`。

## 来源与许可

- 本版源码与发行：https://github.com/hu181b/qq-pet-copilot
- 原仓库：https://github.com/490720818/qq-pet-copilot
- 许可证：GPL-3.0，全文见 LICENSE。
