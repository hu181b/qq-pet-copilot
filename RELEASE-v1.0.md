# QQ 宠物助手 v1.0

本版本基于 [490720818/qq-pet-copilot](https://github.com/490720818/qq-pet-copilot) 二次修改，感谢原作者及上游贡献者。保留原项目的 GPL-3.0 许可证；本仓库提供本发行版对应源码。

## 下载与使用

下载 Release 中的 `QQPetCopilot-v1.0-windows-x64.zip`，解压到有写入权限的文件夹，双击 `QQPetCopilot.exe`。无需安装 Python 或单独下载 ADB、scrcpy、OCR 模型。不要在压缩包预览内直接运行。

支持 Windows x64 和 Android 真机。首次使用请在手机开启 USB 调试，连接电脑并接受手机上的授权，登录 QQ 并打开宠物页面。部分手机还需要允许 USB 调试的输入控制权限；电脑须能识别手机。打开助手后检查连接、设置任务，再点击“开始”。不包含 QQ 客户端、账号或手机权限配置。

首次运行会在 EXE 同目录自动生成配置和运行数据。更新时保留 `config.yaml`、`runs/`、`profiles.json` 和 `profiles/`，替换 EXE 即可。程序不要放在系统保护目录。版本提供自包含便携程序，不是需要安装向导的 MSI。

## 本版本优化

- 移除模拟器支持，专注 Android 真机。
- 护理定位修复：按道具标识识别饼干、香皂，改进状态面板展开/收起和账号、宠物名称识别。
- 打工修复：区分工作面板与背景建筑，减少误选地点；修复好友雇佣工作时长累计。
- 学园导航修复：使用明确入口标识，等待课程面板，避免误开已毕业学园证书；改善慢加载重试。
- 后台资源优化：OCR 使用 CPU 单线程策略，限制数值计算线程；后台停止视频预览并保留无视频关屏进程，托管继续；调度进程降低优先级，减少配置读取和重复状态写盘。
- 包体优化：移除模拟器依赖、未使用的 AVIF/PDF 模块、视频读取库及重复二进制资源。
- 主页支持新建、命名、重命名与切换多套配置；各配置的调度设置、统计、进度和日志分开保存。切换前需停止托管，切换后手动开始。
- 修复镜像闪烁；可拖动四边和四角缩放，保留画面比例，缩窄侧栏、标题栏及画面间距。
- 移除拖动时可能出现的额外系统标题栏，保留窗口缩放和最大化、最小化功能。
- 调度输入框点击后才可编辑，悬停和滚轮不会意外改值。
- “设置 → 关于与更新”可选择右上角 × 关闭程序或最小化；最小化不停止托管，Alt+F4 仍可退出。

后台降耗不能保证在其他软件满载时完全没有资源竞争。游戏更新或手机权限变化可能影响识别和控制。

## 来源与许可

- 原仓库：https://github.com/490720818/qq-pet-copilot
- 本版源码及发布：https://github.com/hu181b/qq-pet-copilot
- 基础提交：`23e8651`，保留上游 Git 历史。
- 许可证全文见 `LICENSE`。本发行版不包含维护者的账号配置、日志、进度和截图数据。
